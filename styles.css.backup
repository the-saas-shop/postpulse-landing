@import url('https://fonts.googleapis.com/css2?family=Bungee&family=Fredoka:wght@300..700&family=Plus+Jakarta+Sans:ital@0;1&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap');

/* Reset and Base Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Roboto', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    line-height: 1.6;
    color: #FFFFFF;
    background-color: #0A0A0A;
    overflow-x: hidden;
}

/* Container */
.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
}

/* Typography */
h1, h2, h3, h4, h5, h6 {
    color: #FFFFFF;
    font-weight: 600;
    line-height: 1.2;
    font-family: 'Bungee', cursive;
}

.hero-heading {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 700;
    margin-bottom: 1.5rem;
    line-height: 1.2;
    color: #FFFFFF;
    font-family: 'Bungee', cursive;
}

.hero-heading .highlight {
    color: #FCD42F;
}

.rotating-text {
    color: #FFFFFF !important;
    background-color: #FCD42F;
    padding: 8px 16px;
    border-radius: 8px;
    position: relative;
    display: inline-block;
    transition: all 0.3s ease;
    box-shadow: 0 2px 8px rgba(252, 212, 47, 0.3);
    font-weight: 600;
}

.rotating-text.fade-out {
    opacity: 0;
    transform: translateY(-2px);
}

.rotating-text::after {
    content: '';
    position: absolute;
    right: -3px;
    top: 0;
    bottom: 0;
    width: 2px;
    background: white;
    animation: blink 1s ease-in-out infinite;
}

.hero-subheading {
    font-size: 1.125rem;
    color: #B0B0B0;
    margin-bottom: 2.5rem;
    max-width: 700px;
    line-height: 1.6;
    margin-left: auto;
    margin-right: auto;
    text-align: center;
    z-index: 2;
    position: relative;
    font-family: 'Roboto', sans-serif;
}

.section-heading {
    font-size: 2rem;
    font-weight: 700;
    text-align: center;
    margin-bottom: 1rem;
    color: #FFFFFF;
    font-family: 'Bungee', cursive;
}

.section-subheading {
    font-size: 1rem;
    color: #B0B0B0;
    text-align: center;
    margin-bottom: 3rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.5;
    font-family: 'Roboto', sans-serif;
}

/* Buttons */
.btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 12px 24px;
    border: none;
    border-radius: 25px;
    font-size: 14px;
    font-weight: 500;
    text-decoration: none;
    cursor: pointer;
    transition: all 0.2s ease-in-out;
    font-family: inherit;
    border: none;
}

.btn-primary, .btn.btn-primary {
    background: #FCD42F !important;
    color: #01112C !important;
    border: none !important;
    font-weight: 600;
}

.btn-primary:hover, .btn.btn-primary:hover {
    background: #E6BF29 !important;
    color: #01112C !important;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(252, 212, 47, 0.4);
}

.btn-accent {
    background: #FCD42F !important;
    color: #01112C !important;
    font-weight: 600;
    border: none;
}

.btn-accent:hover {
    background: #E6BF29 !important;
    color: #01112C !important;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(252, 212, 47, 0.4);
}

.btn-hero {
    padding: 16px 32px;
    font-size: 1rem;
    font-weight: 600;
    background: #FCD42F !important;
    color: #01112C !important;
    border: none;
}

.btn-hero:hover {
    background: #E6BF29 !important;
    color: #01112C !important;
}

.btn-header {
    padding: 12px 24px;
    font-size: 14px;
}

.btn-form {
    padding: 16px 32px;
    font-size: 1rem;
    font-weight: 600;
    width: 100%;
    justify-content: center;
}

/* Header */
.header {
    position: sticky;
    top: 0;
    background: #01112C;
    backdrop-filter: blur(10px);
    border-bottom: 1px solid #333333;
    padding: 16px 0;
    z-index: 100;
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.5rem;
    font-weight: 700;
    color: #FFFFFF;
    font-family: 'Bungee', cursive;
}

.logo i {
    font-size: 1.5rem;
    color: #FCD42F;
}

.nav {
    display: flex;
    gap: 2rem;
}

.nav a {
    color: #B0B0B0;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s ease-in-out;
}

.nav a:hover {
    color: #FCD42F;
}

/* Header Actions */
.header-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
}

/* Hamburger Menu */
.hamburger-menu {
    display: none;
    flex-direction: column;
    justify-content: space-between;
    width: 30px;
    height: 25px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    z-index: 1000;
}

.hamburger-menu span {
    width: 100%;
    height: 3px;
    background-color: #FFFFFF;
    border-radius: 3px;
    transition: all 0.3s ease;
}

.hamburger-menu.active span:nth-child(1) {
    transform: rotate(45deg) translate(9px, 9px);
}

.hamburger-menu.active span:nth-child(2) {
    opacity: 0;
}

.hamburger-menu.active span:nth-child(3) {
    transform: rotate(-45deg) translate(7px, -6px);
}

/* Mobile Menu */
.mobile-menu {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    background: #01112C;
    border-top: 1px solid #333333;
    z-index: 999;
}

.mobile-menu.active {
    display: block;
}

.mobile-menu-content {
    padding: 1rem 0;
}

.mobile-menu-link {
    display: block;
    padding: 1rem 24px;
    color: #B0B0B0;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.2s ease;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.mobile-menu-link:hover {
    color: #FCD42F;
    background-color: rgba(252, 212, 47, 0.1);
}

/* Hero Section */
.hero-section {
    position: relative;
    padding: 120px 0;
    background: #01112C;
    overflow: hidden;
}

.hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
}

.hero-shape {
    position: absolute;
    opacity: 0.05;
}

.hero-shape-1 {
    width: 400px;
    height: 400px;
    background: #FCD42F;
    border-radius: 50%;
    top: -200px;
    left: -200px;
    animation: float 8s ease-in-out infinite;
}

.hero-shape-2 {
    width: 300px;
    height: 300px;
    background: #FCD42F;
    border-radius: 50%;
    top: 20%;
    right: -150px;
    animation: float 6s ease-in-out infinite reverse;
}

.hero-shape-3 {
    width: 250px;
    height: 250px;
    background: #E64848;
    border-radius: 50%;
    bottom: -125px;
    left: 20%;
    animation: float 7s ease-in-out infinite;
}

.hero-shape-4 {
    width: 180px;
    height: 180px;
    background: #FCD42F;
    border-radius: 50%;
    top: 40%;
    left: -90px;
    animation: float 9s ease-in-out infinite;
}

.hero-shape-5 {
    width: 350px;
    height: 350px;
    background: rgba(252, 212, 47, 0.3);
    border-radius: 50%;
    top: 60%;
    right: -175px;
    animation: float 5s ease-in-out infinite reverse;
}

.hero-shape-6 {
    width: 120px;
    height: 120px;
    background: #E64848;
    border-radius: 50%;
    top: 80%;
    right: 15%;
    animation: float 10s ease-in-out infinite;
}

.hero-shape-7 {
    width: 200px;
    height: 40px;
    background: rgba(252, 212, 47, 0.4);
    border-radius: 20px;
    top: 30%;
    right: 25%;
    animation: float 12s ease-in-out infinite;
    transform: rotate(45deg);
}

.hero-shape-8 {
    width: 60px;
    height: 200px;
    background: rgba(230, 72, 72, 0.3);
    border-radius: 30px;
    top: 15%;
    left: 80%;
    animation: float 8s ease-in-out infinite reverse;
    transform: rotate(-30deg);
}

.hero-shape-9 {
    width: 100px;
    height: 100px;
    background: rgba(252, 212, 47, 0.2);
    border-radius: 0;
    top: 70%;
    left: 5%;
    animation: float 11s ease-in-out infinite;
    transform: rotate(45deg);
}

.hero-shape-10 {
    width: 80px;
    height: 80px;
    background: rgba(230, 72, 72, 0.4);
    border-radius: 0;
    top: 10%;
    right: 40%;
    animation: float 9s ease-in-out infinite reverse;
    transform: rotate(60deg);
}

/* Mobile shapes - hidden by default, visible on mobile */
.mobile-shape {
    display: none;
    position: absolute;
    z-index: 1;
    pointer-events: none;
}

.hero-content {
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    min-height: 70vh;
    position: relative;
    gap: 4rem;
}

.hero-text {
    max-width: 800px;
    text-align: center;
    margin: 0 auto;
    z-index: 2;
    position: relative;
    flex: 0 0 auto;
}

.hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(252, 212, 47, 0.1);
    color: #FCD42F;
    padding: 8px 16px;
    border-radius: 25px;
    font-size: 14px;
    font-weight: 500;
    margin-bottom: 2rem;
    border: 1px solid rgba(252, 212, 47, 0.2);
}

.hero-badge i {
    color: #FCD42F;
}

.hero-stats {
    display: flex;
    justify-content: center;
    gap: 3rem;
    margin-top: 3rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 25px;
    padding: 16px 24px;
    backdrop-filter: blur(10px);
    max-width: 500px;
    margin-left: auto;
    margin-right: auto;
}

.stat-item {
    text-align: center;
}

.stat-number {
    font-size: 18px;
    font-weight: 600;
    color: #FCD42F;
    margin-bottom: 4px;
}

.stat-label {
    font-size: 14px;
    color: #B0B0B0;
}

/* Hero Dashboard */
.hero-dashboard {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
    z-index: 1;
}

.dashboard-container {
    position: relative;
    width: 100%;
    height: 100%;
    opacity: 0.3;
    transition: opacity 0.3s ease;
    pointer-events: auto;
}

.dashboard-container:hover {
    opacity: 0.8;
}

.dashboard-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 20px;
    backdrop-filter: blur(10px);
    position: absolute;
    animation: fadeInUp 0.8s ease-out;
    transition: transform 0.3s ease;
}

.dashboard-card:hover {
    transform: translateY(-3px);
}

.main-dashboard {
    width: 320px;
    height: 280px;
    top: 30%;
    left: 5%;
    transform: translateY(-50%);
    z-index: 3;
}

.main-dashboard:hover {
    transform: translateY(-50%) translateY(-3px);
}

.dashboard-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
}

.dashboard-title {
    display: flex;
    align-items: center;
    gap: 8px;
    color: #FFFFFF;
    font-weight: 600;
}

.dashboard-title i {
    color: #FCD42F;
}

.dashboard-status {
    display: flex;
    align-items: center;
    gap: 6px;
    color: #B0B0B0;
    font-size: 12px;
}

.status-dot {
    width: 8px;
    height: 8px;
    background: #FCD42F;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
}

.metric-row {
    display: flex;
    gap: 24px;
    margin-bottom: 20px;
}

.metric {
    flex: 1;
    text-align: center;
}

.metric-label {
    font-size: 12px;
    color: #B0B0B0;
    margin-bottom: 4px;
}

.chart-container {
    margin-top: 16px;
}

.chart-bars {
    display: flex;
    align-items: end;
    gap: 8px;
    height: 60px;
    margin-bottom: 8px;
}

.chart-bar {
    flex: 1;
    background: #333333;
    border-radius: 4px 4px 0 0;
    height: 30px;
}

.chart-labels {
    display: flex;
    justify-content: space-between;
    font-size: 10px;
    color: #B0B0B0;
}

/* Sentiment Analysis Card */
.sentiment-card {
    width: 200px;
    height: 180px;
    top: 10%;
    right: 8%;
    z-index: 2;
}

.card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
}

.card-header h4 {
    font-size: 14px;
    color: #FFFFFF;
    margin: 0;
}

.sentiment-circle {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: #333333;
    display: flex;
    align-items: center;
    justify-content: center;
}

.loading-circle {
    position: relative;
}

.loading-dots {
    display: flex;
    gap: 4px;
}

.loading-dots span {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: #FCD42F;
    animation: loading-bounce 1.4s ease-in-out infinite both;
}

.loading-dots span:nth-child(1) { animation-delay: -0.32s; }
.loading-dots span:nth-child(2) { animation-delay: -0.16s; }

.sentiment-breakdown {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.sentiment-item {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
}

.sentiment-color {
    width: 8px;
    height: 8px;
    border-radius: 50%;
}

.loading-dot {
    background: #333333;
    animation: loading-pulse 1.5s ease-in-out infinite;
}

/* Competitor Analysis Card */
.competitor-card {
    width: 220px;
    height: 160px;
    top: 60%;
    right: 5%;
    z-index: 2;
}

.competitor-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.competitor-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 12px;
}

.competitor-info {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 4px;
}

.competitor-bar {
    width: 60px;
    height: 6px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 3px;
    overflow: hidden;
}

/* Loading States */
.loading-bar {
    background: #333333;
    border-radius: 4px;
    height: 20px;
    width: 100%;
    animation: loading-pulse 1.5s ease-in-out infinite;
}

.loading-bar.small {
    height: 14px;
    width: 80px;
}

.loading-bar.tiny {
    height: 8px;
    width: 40px;
}

/* Floating Elements */
.floating-element {
    position: absolute;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 12px;
    padding: 12px;
    backdrop-filter: blur(10px);
    transition: transform 0.3s ease;
}

.floating-element:hover {
    transform: translateY(-3px);
}

.notification {
    width: 200px;
    bottom: 20%;
    left: 2%;
    display: flex;
    align-items: center;
    gap: 12px;
    z-index: 1;
}

.notification-icon {
    width: 32px;
    height: 32px;
    background: #FCD42F;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #01112C;
    font-size: 14px;
}

.notification-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 4px;
}

.insight-bubble {
    width: 240px;
    bottom: 35%;
    right: 2%;
    display: flex;
    align-items: center;
    gap: 12px;
    z-index: 1;
}

.insight-icon {
    width: 24px;
    height: 24px;
    color: #FCD42F;
    font-size: 16px;
}

.insight-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 4px;
}

/* Problem Section */
.problem-section {
    padding: 64px 0;
    background: #F5F5F5;
}

.problem-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
}

.problem-header {
    text-align: center;
    margin-bottom: 48px;
}

.problem-heading {
    font-size: 32px;
    font-weight: 700;
    line-height: 1.2;
    color: #01112C;
    margin-bottom: 16px;
}

.problem-subheading {
    font-size: 16px;
    font-weight: 400;
    line-height: 1.5;
    color: #01112C;
    max-width: 800px;
    margin: 0 auto;
}

.problem-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 24px;
    margin-top: 48px;
}

.problem-stat {
    background: #FCD42F;
    color: #01112C;
    border-radius: 16px;
    box-shadow: 0 8px 24px rgba(252, 212, 47, 0.25);
    padding: 32px 24px;
    text-align: center;
    transition: all 0.2s ease-in-out;
    position: relative;
    overflow: hidden;
}

.problem-stat:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 32px rgba(252, 212, 47, 0.35);
}

.problem-stat .stat-number {
    font-size: 32px;
    font-weight: 700;
    line-height: 1.2;
    color: #01112C;
    margin-bottom: 16px;
}

.problem-stat .stat-text {
    font-size: 16px;
    font-weight: 400;
    line-height: 1.5;
    color: #01112C;
    max-width: 280px;
    margin: 0 auto;
}

/* Why Lemonoid Section */
.why-lemonoid-section {
    padding: 64px 0;
    background: #FFFFFF;
}

.why-lemonoid-section .section-heading {
    color: #01112C;
}

.why-lemonoid-section .section-subheading {
    color: #01112C;
}

/* How It Works Section */
.how-it-works-section {
    padding: 64px 0;
    background: #01112C;
}

/* New Why Lemonoid Section */
.why-lemonoid-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-top: 48px;
}

.why-lemonoid-card {
    background: #01112C;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    box-shadow: 0 4px 12px rgba(1, 17, 44, 0.3);
    padding: 24px;
    transition: all 0.3s ease-in-out;
}

.why-lemonoid-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(252, 212, 47, 0.3);
    border-color: #FCD42F;
    background: #FCD42F;
}

.why-lemonoid-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 16px;
}

.why-lemonoid-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.why-lemonoid-icon i,
.why-lemonoid-icon svg {
    font-size: 32px;
    width: 32px;
    height: 32px;
    color: #FCD42F;
    fill: #FCD42F;
    transition: all 0.3s ease-in-out;
}

.why-lemonoid-card:hover .why-lemonoid-icon i,
.why-lemonoid-card:hover .why-lemonoid-icon svg {
    color: #01112C;
    fill: #01112C;
}

.why-lemonoid-card h3 {
    font-size: 20px;
    font-weight: 600;
    line-height: 1.4;
    color: #FCD42F;
    margin: 0;
    transition: all 0.3s ease-in-out;
}

.why-lemonoid-card:hover h3 {
    color: #01112C;
}

.why-lemonoid-card p {
    font-size: 16px;
    font-weight: 400;
    line-height: 1.5;
    color: #cbcbcb;
    margin: 0;
    transition: all 0.3s ease-in-out;
}

.why-lemonoid-card:hover p {
    color: #01112C;
}

.section-header {
    text-align: center;
    margin-bottom: 3rem;
}

.features-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
}

.feature-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 32px 24px;
    text-align: center;
    backdrop-filter: blur(10px);
    transition: all 0.2s ease-in-out;
}

.feature-card:hover {
    transform: translateY(-2px);
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(252, 212, 47, 0.3);
}

/* Updated icon sizes - version 2 */
.feature-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem auto;
    transition: all 0.3s ease-in-out;
}

.feature-icon i,
.feature-icon svg {
    font-size: 64px;
    width: 64px;
    height: 64px;
    color: #FCD42F;
    fill: #FCD42F;
    transition: all 0.3s ease-in-out;
}

.feature-card:hover .feature-icon i,
.feature-card:hover .feature-icon svg {
    transform: scale(1.2) rotate(5deg);
    color: #666666;
    fill: #666666;
}

.feature-card h3 {
    font-size: 20px;
    font-weight: 600;
    color: #FFFFFF;
    margin-bottom: 1rem;
}

.feature-card p {
    color: #B0B0B0;
    line-height: 1.5;
}

/* Early Access Section */
.early-access-section {
    padding: 64px 0;
    background: #01112C;
}

.early-access-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
}

.early-access-text .section-heading {
    text-align: left;
    margin-bottom: 2rem;
}

.benefits-list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
}

.benefit-item {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
}

.benefit-icon {
    width: 48px;
    height: 48px;
    background: #FCD42F;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.benefit-icon i {
    font-size: 20px;
    color: #01112C;
}

.benefit-content h3 {
    font-size: 18px;
    font-weight: 600;
    color: #FFFFFF;
    margin-bottom: 0.5rem;
}

.benefit-content p {
    color: #B0B0B0;
    line-height: 1.5;
}

/* Access Card */
.access-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    backdrop-filter: blur(10px);
    overflow: hidden;
    max-width: 400px;
    margin: 0 auto;
}

.access-card-header {
    background: #01112C;
    padding: 20px;
    text-align: center;
}

.access-badge {
    background: #FCD42F;
    color: #01112C;
    padding: 8px 16px;
    border-radius: 25px;
    font-size: 14px;
    font-weight: 600;
    display: inline-block;
}

.access-card-content {
    padding: 24px;
}

.access-feature {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1rem;
}

.access-feature i {
    color: #FCD42F;
    font-size: 18px;
}

.access-feature span {
    color: #FFFFFF;
    font-weight: 500;
}

.access-card-footer {
    background: rgba(255, 255, 255, 0.05);
    padding: 20px;
    text-align: center;
}

.access-price {
    margin-bottom: 0.5rem;
}

.price-original {
    color: #B0B0B0;
    text-decoration: line-through;
    font-size: 14px;
    margin-right: 0.5rem;
}

.price-discounted {
    color: #FCD42F;
    font-size: 24px;
    font-weight: 700;
}

.access-savings {
    color: #188569;
    font-size: 12px;
    font-weight: 600;
}

/* Contact Section */
.contact-section {
    padding: 64px 0;
    background: #01112C;
}

.form-content {
    max-width: 600px;
    margin: 0 auto;
}

.form-header {
    text-align: center;
    margin-bottom: 3rem;
}

.early-access-form {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 32px;
    backdrop-filter: blur(10px);
}

.form-group {
    margin-bottom: 1.5rem;
}

.form-group label {
    display: block;
    font-size: 14px;
    font-weight: 500;
    color: #FFFFFF;
    margin-bottom: 8px;
}

.form-group input,
.form-group select,
.form-group textarea {
    width: 100%;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 25px;
    padding: 12px 16px;
    font-size: 16px;
    color: #FFFFFF;
    transition: all 0.2s ease-in-out;
    font-family: inherit;
}

.form-group input::placeholder,
.form-group textarea::placeholder {
    color: #B0B0B0;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
    outline: none;
    border-color: #FCD42F;
    box-shadow: 0 0 0 3px rgba(252, 212, 47, 0.1);
    background: rgba(255, 255, 255, 0.15);
}

.form-group textarea {
    resize: vertical;
    min-height: 100px;
    border-radius: 16px;
    box-sizing: border-box;
    width: 100% !important;
}

.form-privacy {
    margin-top: 1rem;
    text-align: center;
}

.form-privacy p {
    font-size: 14px;
    color: #B0B0B0;
    line-height: 1.4;
}

/* FAQ Section */
.faq-section {
    padding: 64px 0;
    background: #FFFFFF;
}

.faq-section .section-heading {
    color: #01112C;
}

.faq-section .section-subheading {
    color: #01112C;
}

.faq-container {
    max-width: 800px;
    margin: 0 auto;
}

.faq-item {
    background: #FFFFFF;
    border: 1px solid #E5E5E5;
    border-radius: 12px;
    margin-bottom: 16px;
    overflow: hidden;
    box-shadow: 0 2px 8px rgba(1, 17, 44, 0.1);
    transition: all 0.3s ease;
}

.faq-item:hover {
    box-shadow: 0 4px 16px rgba(1, 17, 44, 0.15);
    border-color: #FCD42F;
}

.faq-question {
    padding: 24px;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: all 0.3s ease;
    background: #FFFFFF;
}

.faq-question:hover {
    background: rgba(252, 212, 47, 0.05);
}

.faq-question h3 {
    color: #01112C;
    font-size: 16px;
    font-weight: 600;
    margin: 0;
    line-height: 1.4;
    font-family: 'Roboto', sans-serif !important;
    flex: 1;
    padding-right: 16px;
}

.faq-question i {
    color: #01112C;
    font-size: 14px;
    transition: transform 0.3s ease;
    flex-shrink: 0;
}

.faq-question.active i {
    transform: rotate(180deg);
    color: #FCD42F;
}

.faq-question.active {
    background: rgba(252, 212, 47, 0.1);
    border-bottom: 1px solid rgba(252, 212, 47, 0.2);
}

.faq-answer {
    padding: 0 24px;
    max-height: 0;
    overflow: hidden;
    transition: all 0.4s ease;
    background: #FFFFFF;
    opacity: 0;
}

.faq-answer.active {
    padding: 12px 24px 24px 24px;
    max-height: 500px;
    opacity: 1;
}

.faq-answer p {
    color: #01112C;
    font-size: 16px;
    line-height: 1.6;
    margin: 0;
    font-family: 'Roboto', sans-serif;
}

/* Footer */
.footer {
    background: #01112C;
    color: #FFFFFF;
    padding: 48px 0 24px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.footer-content {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: 3rem;
    margin-bottom: 2rem;
}

.footer-brand .logo {
    color: #FFFFFF;
    margin-bottom: 1rem;
}

.footer-brand .logo i {
    color: #FCD42F;
}

.footer-brand p {
    color: #B0B0B0;
    line-height: 1.5;
}

.footer-links h4,
.footer-contact h4 {
    color: #FFFFFF;
    font-size: 16px;
    font-weight: 600;
    margin-bottom: 1rem;
}

.footer-links a {
    display: block;
    color: #B0B0B0;
    text-decoration: none;
    margin-bottom: 0.5rem;
    transition: color 0.2s ease-in-out;
}

.footer-links a:hover {
    color: #FCD42F;
}

.contact-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
}

.contact-item i {
    color: #FCD42F;
    width: 16px;
}

.contact-item span {
    color: #B0B0B0;
}

.footer-bottom {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: 1.5rem;
    text-align: center;
}

.footer-bottom p {
    color: #B0B0B0;
    font-size: 14px;
}

/* Form Validation */
.form-group input.error,
.form-group select.error,
.form-group textarea.error {
    border-color: #F44336;
}

.error-message {
    color: #F44336;
    font-size: 14px;
    margin-top: 0.5rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.error-message::before {
    content: "⚠";
    font-size: 14px;
}

/* Enhanced Validation Styles */
.required {
    color: #FCD42F;
    font-weight: 600;
    margin-left: 2px;
}

.form-group.has-error input,
.form-group.has-error select,
.form-group.has-error textarea {
    border-color: #ff6b6b;
    box-shadow: 0 0 0 2px rgba(255, 107, 107, 0.2);
    animation: shake 0.5s ease-in-out;
}

.form-group.has-success input,
.form-group.has-success select,
.form-group.has-success textarea {
    border-color: #51cf66;
    box-shadow: 0 0 0 2px rgba(81, 207, 102, 0.2);
}

.validation-message {
    min-height: 20px;
    font-size: 12px;
    margin-top: 4px;
    opacity: 0;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 4px;
}

.validation-message.show {
    opacity: 1;
}

.validation-message.error {
    color: #ff6b6b;
}

.validation-message.success {
    color: #188569;
}

.validation-message.error::before {
    content: "⚠";
    font-size: 12px;
}

.validation-message.success::before {
    content: "✓";
    font-size: 12px;
}

.form-group {
    transition: all 0.3s ease;
}

.form-group input,
.form-group select,
.form-group textarea {
    transition: all 0.3s ease;
}

@keyframes shake {
    0%, 20%, 40%, 60%, 80% {
        transform: translateX(0);
    }
    10%, 30%, 50%, 70% {
        transform: translateX(-5px);
    }
    15%, 35%, 55%, 75% {
        transform: translateX(5px);
    }
}

.success-message {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: rgba(10, 10, 10, 0.95);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 2rem;
    backdrop-filter: blur(10px);
    z-index: 1000;
    text-align: center;
    max-width: 400px;
    width: 90%;
}

.success-content i {
    font-size: 48px;
    color: #4CAF50;
    margin-bottom: 1rem;
}

.success-content h3 {
    color: #FFFFFF;
    margin-bottom: 1rem;
}

.success-content p {
    color: #B0B0B0;
    line-height: 1.5;
    margin-bottom: 1.5rem;
}

.success-actions {
    display: flex;
    gap: 12px;
    justify-content: center;
    margin-bottom: 1rem;
    flex-wrap: wrap;
}

.success-actions .btn {
    padding: 8px 16px;
    font-size: 14px;
    min-width: 120px;
}

.success-actions .btn-outline {
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: #B0B0B0;
}

.success-actions .btn-outline:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: #FCD42F;
    color: #FCD42F;
}

.success-note {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 8px 16px;
    background: rgba(252, 212, 47, 0.1);
    border: 1px solid rgba(252, 212, 47, 0.3);
    border-radius: 8px;
    font-size: 12px;
    color: #FCD42F;
}

.success-note i {
    font-size: 14px;
}

/* Smooth scrolling */
html {
    scroll-behavior: smooth;
}

/* Animations */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
}

@keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.7; transform: scale(1.1); }
}

@keyframes loading-pulse {
    0%, 100% { 
        opacity: 0.4; 
        background-color: #333333; 
    }
    50% { 
        opacity: 0.8; 
        background-color: #555555; 
    }
}

@keyframes loading-bounce {
    0%, 80%, 100% {
        transform: scale(0);
    } 40% {
        transform: scale(1);
    }
}

@keyframes blink {
    0%, 50% {
        opacity: 1;
    }
    51%, 100% {
        opacity: 0;
    }
}

/* Futuristic Mobile Animations */
@keyframes futuristicPulse {
    0%, 100% {
        transform: scale(1) rotate(0deg);
        opacity: 0.4;
        box-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
    }
    50% {
        transform: scale(1.3) rotate(180deg);
        opacity: 0.8;
        box-shadow: 0 0 40px rgba(0, 255, 255, 0.8), 0 0 60px rgba(252, 212, 47, 0.6);
    }
}

@keyframes neuralPulse {
    0%, 100% {
        transform: scale(1);
        opacity: 0.6;
        box-shadow: 0 0 15px rgba(255, 0, 255, 0.4);
    }
    33% {
        transform: scale(1.2);
        opacity: 0.9;
        box-shadow: 0 0 30px rgba(255, 0, 255, 0.8), 0 0 45px rgba(230, 72, 72, 0.6);
    }
    66% {
        transform: scale(0.9);
        opacity: 0.7;
        box-shadow: 0 0 20px rgba(255, 0, 255, 0.6);
    }
}

@keyframes innerGlow {
    0% {
        opacity: 0.3;
        transform: translate(-50%, -50%) scale(0.8);
    }
    100% {
        opacity: 0.8;
        transform: translate(-50%, -50%) scale(1.2);
    }
}

@keyframes cyberSpin {
    0% {
        transform: rotate(0deg) scale(1);
        filter: drop-shadow(0 0 5px rgba(0, 255, 127, 0.4));
    }
    25% {
        transform: rotate(90deg) scale(1.1);
        filter: drop-shadow(0 0 8px rgba(0, 255, 127, 0.5));
    }
    50% {
        transform: rotate(180deg) scale(1.2);
        filter: drop-shadow(0 0 10px rgba(0, 255, 127, 0.4));
    }
    75% {
        transform: rotate(270deg) scale(1.1);
        filter: drop-shadow(0 0 8px rgba(0, 255, 127, 0.5));
    }
    100% {
        transform: rotate(360deg) scale(1);
        filter: drop-shadow(0 0 5px rgba(0, 255, 127, 0.4));
    }
}

@keyframes holoSlide {
    0% {
        transform: translateX(-30px);
        opacity: 0.3;
    }
    50% {
        transform: translateX(30px);
        opacity: 0.8;
    }
    100% {
        transform: translateX(-30px);
        opacity: 0.3;
    }
}

@keyframes scanLine {
    0% {
        left: -100%;
    }
    100% {
        left: 100%;
    }
}

@keyframes quantumFloat {
    0%, 100% {
        transform: translateY(0px) rotate(0deg);
        opacity: 0.6;
    }
    25% {
        transform: translateY(-15px) rotate(90deg);
        opacity: 0.8;
    }
    50% {
        transform: translateY(-25px) rotate(180deg);
        opacity: 1;
    }
    75% {
        transform: translateY(-15px) rotate(270deg);
        opacity: 0.8;
    }
}

@keyframes orbitRing {
    0% {
        transform: rotate(0deg);
        opacity: 0.3;
    }
    50% {
        opacity: 0.7;
    }
    100% {
        transform: rotate(360deg);
        opacity: 0.3;
    }
}

@keyframes circuitPulse {
    0%, 100% {
        border-color: rgba(0, 255, 255, 0.7);
        box-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
    }
    50% {
        border-color: rgba(0, 255, 255, 1);
        box-shadow: 0 0 25px rgba(0, 255, 255, 0.8), inset 0 0 15px rgba(0, 255, 255, 0.3);
    }
}

@keyframes dataStream {
    0%, 100% {
        transform: translateY(0px) scaleY(1);
        opacity: 0.6;
    }
    50% {
        transform: translateY(-20px) scaleY(1.1);
        opacity: 0.9;
    }
}

@keyframes dataFlow {
    0% {
        top: -20px;
        opacity: 0;
    }
    50% {
        opacity: 1;
    }
    100% {
        top: 100px;
        opacity: 0;
    }
}

@keyframes holoRotate {
    0% {
        transform: rotate(0deg) scale(1);
        opacity: 0.6;
        filter: drop-shadow(0 0 15px rgba(255, 20, 147, 0.6));
    }
    25% {
        transform: rotate(90deg) scale(1.1);
        opacity: 0.8;
        filter: drop-shadow(0 0 25px rgba(255, 20, 147, 0.8));
    }
    50% {
        transform: rotate(180deg) scale(1.2);
        opacity: 1;
        filter: drop-shadow(0 0 35px rgba(255, 20, 147, 1));
    }
    75% {
        transform: rotate(270deg) scale(1.1);
        opacity: 0.8;
        filter: drop-shadow(0 0 25px rgba(255, 20, 147, 0.8));
    }
    100% {
        transform: rotate(360deg) scale(1);
        opacity: 0.6;
        filter: drop-shadow(0 0 15px rgba(255, 20, 147, 0.6));
    }
}

.animate-in {
    animation: fadeInUp 0.6s ease-out;
}

/* Desktop - explicitly hide mobile shapes */
@media (min-width: 769px) {
    .mobile-shape {
        display: none !important;
    }
}

/* Tablet Design */
@media (max-width: 1024px) and (min-width: 769px) {
    .why-lemonoid-grid {
        grid-template-columns: repeat(3, 1fr);
        gap: 20px;
    }
    
    .why-lemonoid-card {
        padding: 20px;
    }
    
    .why-lemonoid-card h3 {
        font-size: 18px;
    }
    
    .why-lemonoid-card p {
        font-size: 15px;
    }
}

/* Responsive Design */
@media (max-width: 768px) {
    .container {
        padding: 0 16px;
    }
    
    .header-content {
        justify-content: space-between;
    }
    
    .nav {
        display: none !important;
    }

    .btn-header {
        display: none !important;
    }

    .header-actions .btn {
        display: none !important;
    }

    .hamburger-menu {
        display: flex !important;
    }
    
    .hero-section {
        padding: 80px 0;
    }

    /* Mobile-specific futuristic animated shapes */
    .mobile-shape {
        display: block;
        position: absolute;
        z-index: 1;
        pointer-events: none;
    }

    /* Futuristic Hexagon with Glow */
    .mobile-shape-1 {
        width: 80px;
        height: 80px;
        background: linear-gradient(45deg, rgba(0, 255, 255, 0.6), rgba(252, 212, 47, 0.4));
        clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
        top: 10%;
        left: 10%;
        animation: futuristicPulse 3s ease-in-out infinite;
        box-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
        filter: blur(0.5px);
    }

    /* Neural Network Node */
    .mobile-shape-2 {
        width: 60px;
        height: 60px;
        background: radial-gradient(circle, rgba(255, 0, 255, 0.8) 0%, rgba(230, 72, 72, 0.3) 70%);
        border-radius: 50%;
        border: 2px solid rgba(255, 0, 255, 0.6);
        top: 20%;
        right: 15%;
        animation: neuralPulse 4s ease-in-out infinite;
        position: relative;
    }

    .mobile-shape-2::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 40px;
        height: 40px;
        background: rgba(255, 255, 255, 0.2);
        border-radius: 50%;
        transform: translate(-50%, -50%);
        animation: innerGlow 2s ease-in-out infinite alternate;
    }

    /* Cyberpunk Triangle */
    .mobile-shape-3 {
        width: 0;
        height: 0;
        border-left: 25px solid transparent;
        border-right: 25px solid transparent;
        border-bottom: 45px solid rgba(0, 255, 127, 0.3);
        top: 40%;
        left: 20%;
        animation: cyberSpin 6s linear infinite;
        filter: drop-shadow(0 0 5px rgba(0, 255, 127, 0.4));
    }

    /* Holographic Bar */
    .mobile-shape-4 {
        width: 100px;
        height: 20px;
        background: linear-gradient(90deg, 
            rgba(252, 212, 47, 0.1) 0%, 
            rgba(252, 212, 47, 0.8) 50%, 
            rgba(252, 212, 47, 0.1) 100%);
        border-radius: 10px;
        border: 1px solid rgba(252, 212, 47, 0.5);
        top: 60%;
        right: 10%;
        animation: holoSlide 5s ease-in-out infinite;
        position: relative;
        overflow: hidden;
    }

    .mobile-shape-4::after {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.6), transparent);
        animation: scanLine 3s linear infinite;
    }

    /* Quantum Orb */
    .mobile-shape-5 {
        width: 50px;
        height: 50px;
        background: radial-gradient(circle at 30% 30%, rgba(138, 43, 226, 0.8), rgba(230, 72, 72, 0.4));
        border-radius: 50%;
        bottom: 30%;
        left: 15%;
        animation: quantumFloat 4s ease-in-out infinite;
        position: relative;
        border: 1px solid rgba(138, 43, 226, 0.6);
    }

    .mobile-shape-5::before {
        content: '';
        position: absolute;
        top: -5px;
        left: -5px;
        right: -5px;
        bottom: -5px;
        border: 2px solid rgba(138, 43, 226, 0.3);
        border-radius: 50%;
        animation: orbitRing 6s linear infinite;
    }

    /* Neon Circuit */
    .mobile-shape-6 {
        width: 70px;
        height: 70px;
        background: transparent;
        border: 2px solid rgba(0, 255, 255, 0.7);
        border-radius: 10px;
        bottom: 15%;
        right: 20%;
        animation: circuitPulse 5s ease-in-out infinite;
        position: relative;
        animation-delay: 1s;
    }

    .mobile-shape-6::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 30px;
        height: 2px;
        background: rgba(0, 255, 255, 0.8);
        transform: translate(-50%, -50%);
        box-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
    }

    .mobile-shape-6::after {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 2px;
        height: 30px;
        background: rgba(0, 255, 255, 0.8);
        transform: translate(-50%, -50%);
        box-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
    }

    /* Data Stream */
    .mobile-shape-7 {
        width: 30px;
        height: 80px;
        background: linear-gradient(180deg, 
            rgba(0, 255, 255, 0.8) 0%, 
            rgba(0, 255, 255, 0.4) 50%, 
            rgba(0, 255, 255, 0.1) 100%);
        border-radius: 15px;
        top: 70%;
        left: 50%;
        animation: dataStream 6s ease-in-out infinite;
        animation-delay: 2s;
        position: relative;
        overflow: hidden;
    }

    .mobile-shape-7::before {
        content: '';
        position: absolute;
        top: -20px;
        left: 0;
        width: 100%;
        height: 20px;
        background: rgba(255, 255, 255, 0.6);
        animation: dataFlow 2s linear infinite;
    }

    /* Holographic Diamond */
    .mobile-shape-8 {
        width: 45px;
        height: 45px;
        background: linear-gradient(45deg, rgba(255, 20, 147, 0.6), rgba(252, 212, 47, 0.4));
        top: 25%;
        left: 70%;
        animation: holoRotate 7s linear infinite;
        animation-delay: 1.5s;
        clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
        filter: drop-shadow(0 0 15px rgba(255, 20, 147, 0.6));
    }
    
    .hero-content {
        flex-direction: column;
        gap: 2rem;
        min-height: 60vh;
    }
    
    .hero-dashboard {
        display: none;
    }
    
    .dashboard-container {
        height: 100%;
        transform: scale(0.7);
        opacity: 0.6;
    }
    
    .main-dashboard,
    .sentiment-card,
    .competitor-card,
    .notification,
    .insight-bubble {
        display: none;
    }
    
    .hero-heading {
        font-size: 2rem;
    }
    
    .rotating-text::after {
        width: 1px;
        right: -2px;
    }
    
    .rotating-text {
        padding: 6px 12px;
        font-size: 0.9em;
    }
    
    .hero-subheading {
        font-size: 1rem;
    }
    
    .hero-stats {
        flex-direction: column;
        gap: 1rem;
        padding: 16px;
    }
    
    .section-heading {
        font-size: 1.75rem;
    }
    
    .features-grid {
        grid-template-columns: 1fr;
    }
    
    .early-access-content {
        grid-template-columns: 1fr;
        gap: 2rem;
    }
    
    .early-access-text .section-heading {
        text-align: center;
    }
    
    .footer-content {
        grid-template-columns: 1fr;
        gap: 2rem;
    }
    
    .btn-hero {
        padding: 14px 24px;
        font-size: 0.875rem;
    }
    
    .hero-badge {
        font-size: 12px;
        padding: 6px 12px;
    }
    
    .feature-card {
        padding: 24px 16px;
    }

    .problem-stats {
        grid-template-columns: 1fr;
        gap: 16px;
    }

    .problem-stat {
        padding: 24px 16px;
    }

    .problem-heading {
        font-size: 24px;
    }

    .problem-subheading {
        font-size: 14px;
    }

    .why-lemonoid-grid {
        grid-template-columns: 1fr;
        gap: 16px;
    }

    .why-lemonoid-card {
        padding: 20px;
    }

    .why-lemonoid-header {
        gap: 10px;
    }

    .why-lemonoid-card h3 {
        font-size: 18px;
    }

    .why-lemonoid-card p {
        font-size: 14px;
    }

    .why-lemonoid-icon i,
    .why-lemonoid-icon svg {
        font-size: 28px;
        width: 28px;
        height: 28px;
    }
}

@media (max-width: 480px) {
    .nav {
        display: none !important;
    }

    .btn-header {
        display: none !important;
    }

    .header-actions .btn {
        display: none !important;
    }

    .hamburger-menu {
        display: flex !important;
    }
    
    .hero-section {
        padding: 60px 0;
    }

    /* Mobile-specific futuristic animated shapes - smaller sizes for smaller screens */
    .mobile-shape {
        display: block;
        position: absolute;
        z-index: 1;
        pointer-events: none;
    }

    /* Futuristic Hexagon with Glow - smaller for 480px */
    .mobile-shape-1 {
        width: 60px;
        height: 60px;
        background: linear-gradient(45deg, rgba(0, 255, 255, 0.6), rgba(252, 212, 47, 0.4));
        clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
        top: 15%;
        left: 10%;
        animation: futuristicPulse 3s ease-in-out infinite;
        box-shadow: 0 0 15px rgba(0, 255, 255, 0.5);
        filter: blur(0.5px);
    }

    /* Neural Network Node - smaller for 480px */
    .mobile-shape-2 {
        width: 45px;
        height: 45px;
        background: radial-gradient(circle, rgba(255, 0, 255, 0.8) 0%, rgba(230, 72, 72, 0.3) 70%);
        border-radius: 50%;
        border: 1px solid rgba(255, 0, 255, 0.6);
        top: 25%;
        right: 15%;
        animation: neuralPulse 4s ease-in-out infinite;
        position: relative;
    }

    .mobile-shape-2::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 25px;
        height: 25px;
        background: rgba(255, 255, 255, 0.2);
        border-radius: 50%;
        transform: translate(-50%, -50%);
        animation: innerGlow 2s ease-in-out infinite alternate;
    }

    /* Cyberpunk Triangle - smaller for 480px */
    .mobile-shape-3 {
        width: 0;
        height: 0;
        border-left: 18px solid transparent;
        border-right: 18px solid transparent;
        border-bottom: 32px solid rgba(0, 255, 127, 0.3);
        top: 45%;
        left: 25%;
        animation: cyberSpin 6s linear infinite;
        filter: drop-shadow(0 0 4px rgba(0, 255, 127, 0.4));
    }

    /* Holographic Bar - smaller for 480px */
    .mobile-shape-4 {
        width: 80px;
        height: 15px;
        background: linear-gradient(90deg, 
            rgba(252, 212, 47, 0.1) 0%, 
            rgba(252, 212, 47, 0.8) 50%, 
            rgba(252, 212, 47, 0.1) 100%);
        border-radius: 8px;
        border: 1px solid rgba(252, 212, 47, 0.5);
        top: 65%;
        right: 10%;
        animation: holoSlide 5s ease-in-out infinite;
        position: relative;
        overflow: hidden;
    }

    .mobile-shape-4::after {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.6), transparent);
        animation: scanLine 3s linear infinite;
    }

    /* Quantum Orb - smaller for 480px */
    .mobile-shape-5 {
        width: 40px;
        height: 40px;
        background: radial-gradient(circle at 30% 30%, rgba(138, 43, 226, 0.8), rgba(230, 72, 72, 0.4));
        border-radius: 50%;
        bottom: 35%;
        left: 15%;
        animation: quantumFloat 4s ease-in-out infinite;
        position: relative;
        border: 1px solid rgba(138, 43, 226, 0.6);
    }

    .mobile-shape-5::before {
        content: '';
        position: absolute;
        top: -4px;
        left: -4px;
        right: -4px;
        bottom: -4px;
        border: 1px solid rgba(138, 43, 226, 0.3);
        border-radius: 50%;
        animation: orbitRing 6s linear infinite;
    }

    /* Neon Circuit - smaller for 480px */
    .mobile-shape-6 {
        width: 55px;
        height: 55px;
        background: transparent;
        border: 2px solid rgba(0, 255, 255, 0.7);
        border-radius: 8px;
        bottom: 20%;
        right: 20%;
        animation: circuitPulse 5s ease-in-out infinite;
        position: relative;
        animation-delay: 1s;
    }

    .mobile-shape-6::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 20px;
        height: 2px;
        background: rgba(0, 255, 255, 0.8);
        transform: translate(-50%, -50%);
        box-shadow: 0 0 8px rgba(0, 255, 255, 0.5);
    }

    .mobile-shape-6::after {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 2px;
        height: 20px;
        background: rgba(0, 255, 255, 0.8);
        transform: translate(-50%, -50%);
        box-shadow: 0 0 8px rgba(0, 255, 255, 0.5);
    }

    /* Data Stream - smaller for 480px */
    .mobile-shape-7 {
        width: 25px;
        height: 60px;
        background: linear-gradient(180deg, 
            rgba(0, 255, 255, 0.8) 0%, 
            rgba(0, 255, 255, 0.4) 50%, 
            rgba(0, 255, 255, 0.1) 100%);
        border-radius: 12px;
        top: 75%;
        left: 50%;
        animation: dataStream 6s ease-in-out infinite;
        animation-delay: 2s;
        position: relative;
        overflow: hidden;
    }

    .mobile-shape-7::before {
        content: '';
        position: absolute;
        top: -15px;
        left: 0;
        width: 100%;
        height: 15px;
        background: rgba(255, 255, 255, 0.6);
        animation: dataFlow 2s linear infinite;
    }

    /* Holographic Diamond - smaller for 480px */
    .mobile-shape-8 {
        width: 35px;
        height: 35px;
        background: linear-gradient(45deg, rgba(255, 20, 147, 0.6), rgba(252, 212, 47, 0.4));
        top: 30%;
        left: 70%;
        animation: holoRotate 7s linear infinite;
        animation-delay: 1.5s;
        clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
        filter: drop-shadow(0 0 12px rgba(255, 20, 147, 0.6));
    }
    
    .hero-heading {
        font-size: 1.75rem;
    }
    
    .rotating-text::after {
        width: 1px;
        right: -1px;
    }
    
    .rotating-text {
        padding: 4px 8px;
        font-size: 0.85em;
    }
    
    .section-heading {
        font-size: 1.5rem;
    }
    
    .feature-icon i,
    .feature-icon svg {
        font-size: 48px;
        width: 48px;
        height: 48px;
    }
    
    .early-access-form {
        padding: 24px;
    }
    
    .hero-stats {
        gap: 0.5rem;
    }
    
    .stat-item {
        font-size: 0.875rem;
    }
    
    .dashboard-container {
        height: 100%;
        transform: scale(0.6);
    }
    
    .main-dashboard,
    .sentiment-card,
    .competitor-card,
    .notification,
    .insight-bubble {
        display: none;
    }

    .problem-section {
        padding: 48px 0;
    }

    .problem-heading {
        font-size: 20px;
    }

    .problem-subheading {
        font-size: 14px;
    }

    .problem-stat {
        padding: 16px;
    }

    .problem-stat .stat-number {
        font-size: 24px;
    }

    .problem-stat .stat-text {
        font-size: 14px;
    }

    .why-lemonoid-card {
        padding: 16px;
    }

    .why-lemonoid-header {
        gap: 8px;
        margin-bottom: 12px;
    }

    .why-lemonoid-card h3 {
        font-size: 16px;
    }

    .why-lemonoid-card p {
        font-size: 14px;
    }

    .why-lemonoid-icon i,
    .why-lemonoid-icon svg {
        font-size: 24px;
        width: 24px;
        height: 24px;
    }

    .faq-section {
        padding: 48px 0;
    }

    .faq-question {
        padding: 20px;
    }

    .faq-question h3 {
        font-size: 14px !important;
        font-family: 'Roboto', sans-serif !important;
        padding-right: 12px;
    }

    .faq-answer.active {
        padding: 8px 20px 20px 20px;
    }

    .faq-answer p {
        font-size: 14px;
    }
} 